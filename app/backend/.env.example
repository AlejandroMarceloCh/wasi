# Configuración Justa Backend
# Base de datos: si no se define DATABASE_URL se usa SQLite (cero setup).
# Para PostgreSQL: DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/justa
JWT_SECRET=cambia_este_secreto_en_produccion_por_uno_largo_y_aleatorio
JWT_ALGO=HS256
JWT_EXPIRE_DAYS=7
