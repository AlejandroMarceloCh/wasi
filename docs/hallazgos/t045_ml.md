# T045 — ml: `predict_fair_value` (veredicto, clamps, fallbacks)

**TARGET:** `src/wasi/models/ml.py`  
**LENTE:** correctitud  
**Fecha:** 2026-07-06

---

## Resumen

Pipeline geo → features → predicción → zona/confianza está **coherente** y alineado con tests (`test_ml.py`, `test_v2_features.py`). Los fallbacks de confianza y la banda ±8 % funcionan. Riesgos: umbrales de confianza con fallback silencioso, métricas hardcodeadas y zona “Justo” cuando `fair_value=0`.

---

## Hallazgos

### [INFO] · Banda de veredicto ±8 % · `ml.py:58,596-601` — Ganga / Justo / Inflado por `diff_pct` vs `ZONE_BAND_PCT`. · Consistente con venta y listings. · OK.

### [INFO] · Confianza calibrada por densidad + fallback geo · `ml.py:401-419` — `fallback_reason`, proximidad al training y umbrales `CONF_ALTA_MIN` / `CONF_MEDIA_MIN`. · Test `test_predict_low_coverage_marca_confianza_baja`. · OK.

### [INFO] · Warning de leakage en catálogo / pin sobre listing · `ml.py:393-399,609-612` — `TRAIN_PROXIMITY_KM=0.05`, flag `from_catalog`. · Honestidad del veredicto. · OK.

### [MEDIO] · Umbrales de confianza: `except Exception` silencioso · `ml.py:25-30` — si falla lectura de `confidence_thresholds.json`, usa 119/27 hardcodeados sin log. · Prod con path roto sigue sirviendo con umbrales posiblemente obsoletos. · Log warning + métrica; fail en startup si el JSON es crítico.

### [MEDIO] · Métricas R²/MAE hardcodeadas por modo · `ml.py:60-65,623-625` — no se leen del bundle ni de `metricas_test` del joblib v2. · Health/UI pueden mostrar métricas desincronizadas tras re-entrenar. · Exponer desde `model_service` post-load.

### [MEDIO] · `fair_value=0` → `diff_pct=0` y zona “Justo” · `ml.py:594-601` — evita división por cero pero enmascara precio anunciado vs referencia nula. · Caso patológico (modelo roto) parece “precio justo”. · Si `fair_value <= 0`: confidence=Baja, zone=None, warning explícito.

### [INFO] · Counterfactuals con base P50 si hay quantile · `ml.py:606-607,591` — coherencia centro/rango. · OK.

### [INFO] · Ruteo v1/v2 en un solo entry-point · `ml.py:584-588` — `build_features_v2` vs `build_features`. · OK.

### [BAJO] · `_factores` heurísticos (no del modelo) · `ml.py:370-391` — scores UX decorativos. · No afectan veredicto; puede confundir si se interpretan como SHAP. · Etiquetar como “indicadores orientativos” en API/UI.

### [INFO] · OutOfBounds propagado · `ml.py:582,578` — docstring indica excepción; router mapea 400. · OK.

---

## Veredicto

**Veredicto y confianza bien diseñados** para el flujo normal. Endurecer **fallback de umbrales**, **métricas runtime** y el caso **`fair_value=0`**.
