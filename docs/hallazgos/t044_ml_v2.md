# T044 — ml_v2: `build_features_v2` (feature_order e inputs raros)

**TARGET:** `src/wasi/models/ml_v2.py`  
**LENTE:** correctitud  
**Fecha:** 2026-07-06

---

## Resumen

El ensamblado en orden canónico con defaults a 0 es **correcto** cuando el modelo ya está cargado y el form viene del schema Pydantic. Inputs incompletos, modelo no cargado o valores extremos pueden producir **KeyError**, DataFrame vacío o features implausibles sin validación local.

---

## Hallazgos

### [INFO] · Inicialización en orden canónico · `ml_v2.py:48,121-122` — dict pre-rellenado con `model_service.feature_order`, salida 1×N alineada. · Paridad train/serve. · OK; test `test_build_features_v2_shape_y_columnas_clave`.

### [INFO] · `_set` ignora features fu fuera del orden · `ml_v2.py:124-127` — no truena si el modelo pierde una columna legacy. · Forward-compat. · OK.

### [MEDIO] · Campos estructurales obligatorios sin `.get()` · `ml_v2.py:50-54,63-64` — `form["area"]`, `form["lat"]`, etc. → `KeyError` si falta clave. · Llamadas internas o scripts que omitan un campo revientan antes del router. · Validar al inicio o documentar contrato estricto; alinear con `build_features` v1 que usa el mismo patrón.

### [MEDIO] · Modelo no cargado → DataFrame 0 columnas · `ml_v2.py:48` + `model_service.feature_order` (`model_service.py:291-293` devuelve `[]` si unloaded). · `predict()` falla opaco en arranque parcial o tests sin `load()`. · Guard clause: `if not model_service.is_loaded: raise RuntimeError(...)`.

### [MEDIO] · Valores negativos no se clampan antes de derivadas · `ml_v2.py:50-54,106-108` — área/dorm/baños negativos producen ratios negativos; solo el paso log1p usa `max(..., 0)` (`ml_v2.py:118-119`). · Predicciones absurdas si el cliente envía datos corruptos. · Clamps en schema (ya parcial) + assert en build para área/antigüedad ≥ 0.

### [BAJO] · OHE fijas: siempre departamento / mismatch ninguno · `ml_v2.py:115-116` — no hay rama para casa/oficina. · Sesgo conocido del producto (solo deptos). · Documentar; ampliar cuando el modelo lo soporte.

### [BAJO] · Rama redundante en target encoder · `ml_v2.py:83-86` — `hasattr(enc["map"], "get")` y rama `else` hacen lo mismo. · Ruido en mantenimiento. · Simplificar a una línea como `ml.py:129-130`.

### [INFO] · OSM + distrito features integrados · `ml_v2.py:89-104` — `get_osm().lookup` y `get_distrito_features().lookup(distrito)`. · Pipeline v2 completo. · OK; tests v2 en `test_v2_features.py`.

### [INFO] · Sin NaN/Inf en camino feliz · `ml_v2.py:118-122` — test `test_build_features_v2_sin_nan_ni_inf`. · OK.

---

## Veredicto

**Feature engineering v2 correcto** con form válido y modelo cargado. Endurecer **contrato de entrada** y **guard de modelo cargado** para inputs raros fuera del happy path del API.
